import React from "react";
import styles from "./navContactInfo.module.scss";
const NavContactInfo = (props) => {
  return (
    <div className={styles.navInfo}>
      <div className={styles.typing}>Wanna join me?</div>
    </div>
  );
};

export default NavContactInfo;
