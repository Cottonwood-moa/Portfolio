import React from "react";
import styles from "./navAboutInfo.module.css";
const NavAboutInfo = (props) => {
  return (
    <div className={styles.navInfo}>
      <h1>About info</h1>
    </div>
  );
};

export default NavAboutInfo;
