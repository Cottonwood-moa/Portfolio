import React from "react";
import styles from "./dependency.module.scss";
const Dependency = (props) => {
  return <div className={styles.dependencyContainer}>Dependency</div>;
};

export default Dependency;
